<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  <body>
    <h1><%= title %></h1>
    <% if (foto) { %>
      <h2><%= foto.titulo %></h2>
      <p><strong>Descripción:</strong> <%= foto.descripcion %></p>
      <p><strong>Calificación:</strong> <%= foto.calificacion %></p>
      <p><strong>Ruta:</strong> <%= foto.ruta %></p>
      <% if (foto.ruta) { %>
        <img src="/<%= foto.ruta %>" alt="<%= foto.titulo %>" style="max-width:300px;" />
      <% } %>
    <% } else { %>
      <p>Foto no encontrada.</p>
    <% } %>
    <a href="/fotos/findAll/view">Volver a la lista</a>
  </body>
</html> 